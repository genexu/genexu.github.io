---
interface Props {
	datetime: Date;
	lang: string;
}

const { datetime, lang } = Astro.props;
---

<time class="text-xs italic" datetime={datetime.toISOString()}>
	{
		datetime.toLocaleDateString(lang, {
			year: "numeric",
			month: "short",
			day: "numeric",
		})
	}
</time>
