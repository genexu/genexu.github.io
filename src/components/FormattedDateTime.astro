---
interface Props {
	datetime: Date;
	lang: string;
}

const { datetime, lang } = Astro.props;
---

<time datetime={datetime.toISOString()}>
	{
		datetime.toLocaleDateString(lang, {
			year: "numeric",
			month: "short",
			day: "numeric",
		})
	}
</time>
